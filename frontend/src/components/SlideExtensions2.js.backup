import { motion } from 'framer-motion';
import { Users, DollarSign, TrendingUp, BarChart3, Clock, Target, Settings, AlertCircle, CheckCircle, Share2, Globe, Calendar } from 'lucide-react';

// Parte 2: Slides das Engrenagens e outros tipos complexos
export const renderEngrenagensSlides = (slide, textSize, getTextSize, fadeIn) => {

  // SLIDE: ENGRENAGEM 1 - PRODUTO
  if (slide.type === 'gear-product') {
    return (
      <div className="h-full flex flex-col bg-black">
        <motion.h2 {...fadeIn} className="text-6xl font-bold text-white pt-12 pb-8 text-center">
          {slide.title}
        </motion.h2>

        <div className="flex-1 overflow-y-auto scrollbar-hide px-12 pb-12">
          <div className="space-y-8 max-w-5xl mx-auto">
            {slide.sections.map((section, i) => (
              <motion.div
                key={i}
                initial={{ opacity: 0, x: -30 }}
                animate={{ opacity: 1, x: 0 }}
                transition={{ delay: 0.2 + i * 0.15 }}
                className={`${
                  section.highlight
                    ? 'bg-gradient-to-r from-red-900 to-red-800 border-2 border-red-500'
                    : 'bg-gray-900 border border-gray-800'
                } rounded-xl p-8`}
              >
                <h3 className={`text-3xl font-bold mb-6 ${section.highlight ? 'text-white' : 'text-red-400'}`}>
                  {section.title}
                </h3>
                <div className={`${getTextSize('lg')} text-gray-300 space-y-3`}>
                  {section.items.map((item, j) => (
                    <p key={j} className={section.highlight && j > 0 ? 'pl-6' : ''}>{item}</p>
                  ))}
                </div>
              </motion.div>
            ))}
          </div>
        </div>
      </div>
    );
  }

  // SLIDE: ORGANOGRAMA
  if (slide.type === 'organogram') {
    return (
      <div className="h-full flex flex-col bg-black">
        <motion.h2 {...fadeIn} className="text-6xl font-bold text-white pt-12 pb-8 text-center">
          {slide.title}
        </motion.h2>

        <div className="flex-1 overflow-y-auto scrollbar-hide px-12 pb-12">
          <div className="max-w-6xl mx-auto space-y-8">
            {/* Gestor */}
            <motion.div
              initial={{ opacity: 0, y: -20 }}
              animate={{ opacity: 1, y: 0 }}
              transition={{ delay: 0.2 }}
              className="bg-gradient-to-r from-red-900 to-red-700 border-2 border-red-500 rounded-xl p-6 text-center"
            >
              <h3 className="text-4xl font-bold text-white">{slide.organogram.gestor}</h3>
            </motion.div>

            {/* Áreas */}
            <div className="grid grid-cols-3 gap-6">
              {slide.organogram.areas.map((area, i) => (
                <motion.div
                  key={i}
                  initial={{ opacity: 0, y: 20 }}
                  animate={{ opacity: 1, y: 0 }}
                  transition={{ delay: 0.4 + i * 0.1 }}
                  className="bg-gray-900 border border-gray-700 rounded-xl p-6 text-center"
                >
                  <h4 className="text-2xl font-bold text-red-400 mb-3">{area.nome}</h4>
                  <p className={`${getTextSize('base')} text-gray-300 mb-2`}>{area.pessoas}</p>
                  <p className="text-3xl font-bold text-white">{area.custo}</p>
                </motion.div>
              ))}
            </div>

            {/* Extras e Total */}
            <motion.div
              initial={{ opacity: 0 }}
              animate={{ opacity: 1 }}
              transition={{ delay: 0.8 }}
              className="space-y-6"
            >
              <div className="bg-gray-900 border border-gray-700 rounded-xl p-6 text-center">
                <p className={`${getTextSize('lg')} text-gray-300 whitespace-pre-line`}>
                  {slide.organogram.extras}
                </p>
              </div>

              <div className="bg-gradient-to-r from-green-900/30 to-transparent border-2 border-green-500 rounded-xl p-6">
                <div className="grid grid-cols-3 gap-6 text-center">
                  <div>
                    <p className="text-gray-400 mb-2">TOTAL FOLHA</p>
                    <p className="text-3xl font-bold text-white">{slide.organogram.total.folha}</p>
                  </div>
                  <div>
                    <p className="text-gray-400 mb-2">+ ENCARGOS</p>
                    <p className="text-3xl font-bold text-white">{slide.organogram.total.encargos}</p>
                  </div>
                  <div>
                    <p className="text-gray-400 mb-2">TOTAL</p>
                    <p className="text-4xl font-bold text-green-400">{slide.organogram.total.total}</p>
                  </div>
                </div>
              </div>
            </motion.div>

            {/* Decisões */}
            <motion.div
              initial={{ opacity: 0 }}
              animate={{ opacity: 1 }}
              transition={{ delay: 1 }}
              className="bg-blue-900/20 border border-blue-700 rounded-xl p-6"
            >
              <h4 className="text-2xl font-bold text-blue-400 mb-4 text-center">DECISÕES</h4>
              <div className={`${getTextSize('lg')} text-gray-300 grid grid-cols-2 gap-4`}>
                {slide.decisoes.map((decisao, i) => (
                  <p key={i}>• {decisao}</p>
                ))}
              </div>
            </motion.div>
          </div>
        </div>
      </div>
    );
  }

  // SLIDE: CASHFLOW
  if (slide.type === 'cashflow') {
    return (
      <div className="h-full flex flex-col bg-black">
        <motion.h2 {...fadeIn} className="text-6xl font-bold text-white pt-12 pb-8 text-center">
          {slide.title}
        </motion.h2>

        <div className="flex-1 overflow-y-auto scrollbar-hide px-12 pb-12">
          <div className="grid grid-cols-2 gap-8 max-w-6xl mx-auto">
          {/* Entrada */}
          <motion.div
            initial={{ opacity: 0, x: -30 }}
            animate={{ opacity: 1, x: 0 }}
            transition={{ delay: 0.2 }}
            className="space-y-6"
          >
            <div className="bg-green-900/30 border-2 border-green-500 rounded-xl p-6">
              <h3 className="text-3xl font-bold text-green-400 mb-4">{slide.entrada.title}</h3>
              <div className={`${getTextSize('lg')} text-gray-300 space-y-3`}>
                {slide.entrada.items.map((item, i) => (
                  <p key={i}>• {item}</p>
                ))}
              </div>
            </div>

            <div className="bg-red-900/30 border-2 border-red-500 rounded-xl p-6">
              <h3 className="text-3xl font-bold text-red-400 mb-4">{slide.saida.title}</h3>
              <div className={`${getTextSize('lg')} text-gray-300 space-y-3`}>
                {slide.saida.items.map((item, i) => (
                  <p key={i}>{item}</p>
                ))}
              </div>
            </div>
          </motion.div>

          {/* Controle e Alçadas */}
          <motion.div
            initial={{ opacity: 0, x: 30 }}
            animate={{ opacity: 1, x: 0 }}
            transition={{ delay: 0.4 }}
            className="space-y-6"
          >
            <div className="bg-blue-900/30 border-2 border-blue-500 rounded-xl p-6">
              <h3 className="text-3xl font-bold text-blue-400 mb-4">{slide.controle.title}</h3>
              <div className={`${getTextSize('lg')} text-gray-300 space-y-3`}>
                {slide.controle.items.map((item, i) => (
                  <p key={i}>{item}</p>
                ))}
              </div>
            </div>

            <div className="bg-amber-900/30 border-2 border-amber-500 rounded-xl p-6">
              <h3 className="text-3xl font-bold text-amber-400 mb-4">{slide.alcadas.title}</h3>
              <div className={`${getTextSize('lg')} text-gray-300 space-y-3`}>
                {slide.alcadas.items.map((item, i) => (
                  <p key={i}>{item}</p>
                ))}
              </div>
            </div>
          </motion.div>
          </div>
        </div>
      </div>
    );
  }

  // SLIDE: MANAGEMENT
  if (slide.type === 'management') {
    const managementIconMap = {
      '📊': BarChart3,
      '💰': DollarSign,
      '⚙️': Settings,
      '🎯': Target,
    };

    return (
      <div className="h-full flex flex-col bg-black">
        <motion.h2 {...fadeIn} className="text-6xl font-bold text-white pt-12 pb-8 text-center">
          {slide.title}
        </motion.h2>

        <div className="flex-1 overflow-y-auto scrollbar-hide px-12 pb-12">
          <div className="max-w-6xl mx-auto">
          {/* Controles */}
          <div className="grid grid-cols-3 gap-6 mb-12">
            {slide.controls.map((control, i) => {
              const ControlIcon = managementIconMap[control.icon] || BarChart3;
              return (
                <motion.div
                  key={i}
                  initial={{ opacity: 0, y: 20 }}
                  animate={{ opacity: 1, y: 0 }}
                  transition={{ delay: 0.2 + i * 0.1 }}
                  className="bg-gray-900 border border-gray-700 rounded-xl p-6 text-center"
                >
                  <ControlIcon className="w-16 h-16 mx-auto mb-4 text-red-400" />
                  <h4 className="text-2xl font-bold text-red-400 mb-4">{control.title}</h4>
                  <div className={`${getTextSize('base')} text-gray-300 space-y-2`}>
                    {control.items.map((item, j) => (
                      <p key={j}>{item}</p>
                    ))}
                  </div>
                </motion.div>
              );
            })}
          </div>

          {/* KPIs */}
          <motion.div
            initial={{ opacity: 0, y: 20 }}
            animate={{ opacity: 1, y: 0 }}
            transition={{ delay: 0.6 }}
            className="bg-gradient-to-r from-green-900/30 to-transparent border-2 border-green-500 rounded-xl p-8"
          >
            <h3 className="text-3xl font-bold text-green-400 mb-8 text-center">{slide.kpis.title}</h3>
            <div className="grid grid-cols-5 gap-6">
              {slide.kpis.items.map((kpi, i) => (
                <div key={i} className="text-center">
                  <p className={`${getTextSize('base')} text-gray-400 mb-3`}>{kpi.label}</p>
                  <p className="text-3xl font-bold text-white">{kpi.target}</p>
                </div>
              ))}
            </div>
          </motion.div>
          </div>
        </div>
      </div>
    );
  }

  // SLIDE: MARKETING
  if (slide.type === 'marketing') {
    return (
      <div className="h-full flex flex-col bg-black">
        <motion.h2 {...fadeIn} className="text-6xl font-bold text-white pt-12 pb-8 text-center">
          {slide.title}
        </motion.h2>

        <div className="flex-1 overflow-y-auto scrollbar-hide px-12 pb-12">
          <div className="max-w-6xl mx-auto space-y-8">
          {/* Digital e Offline */}
          <div className="grid grid-cols-2 gap-8">
            <motion.div
              initial={{ opacity: 0, x: -30 }}
              animate={{ opacity: 1, x: 0 }}
              transition={{ delay: 0.2 }}
              className="bg-gradient-to-br from-blue-900/30 to-transparent border-2 border-blue-500 rounded-xl p-6"
            >
              <h3 className="text-3xl font-bold text-blue-400 mb-6">{slide.digital.title}</h3>
              <div className={`${getTextSize('lg')} text-gray-300 space-y-3`}>
                {slide.digital.items.map((item, i) => (
                  <p key={i}>{item}</p>
                ))}
              </div>
            </motion.div>

            <motion.div
              initial={{ opacity: 0, x: 30 }}
              animate={{ opacity: 1, x: 0 }}
              transition={{ delay: 0.3 }}
              className="bg-gradient-to-br from-amber-900/30 to-transparent border-2 border-amber-500 rounded-xl p-6"
            >
              <h3 className="text-3xl font-bold text-amber-400 mb-6">{slide.offline.title}</h3>
              <div className={`${getTextSize('lg')} text-gray-300 space-y-3`}>
                {slide.offline.items.map((item, i) => (
                  <p key={i}>{item}</p>
                ))}
              </div>
            </motion.div>
          </div>

          {/* Investimento e Retorno */}
          <motion.div
            initial={{ opacity: 0, y: 20 }}
            animate={{ opacity: 1, y: 0 }}
            transition={{ delay: 0.5 }}
            className="grid grid-cols-2 gap-6"
          >
            <div className="bg-gray-900 border border-gray-700 rounded-xl p-6 text-center">
              <div className="flex items-center justify-center gap-2 mb-3">
                <DollarSign className="w-6 h-6 text-red-400" />
                <h4 className="text-2xl font-bold text-red-400">INVESTIMENTO</h4>
              </div>
              <p className="text-4xl font-bold text-white mb-2">{slide.investimento.value}</p>
              <p className={`${getTextSize('base')} text-gray-400`}>{slide.investimento.note}</p>
            </div>
            <div className="bg-gray-900 border border-gray-700 rounded-xl p-6 text-center">
              <div className="flex items-center justify-center gap-2 mb-3">
                <TrendingUp className="w-6 h-6 text-green-400" />
                <h4 className="text-2xl font-bold text-green-400">RETORNO</h4>
              </div>
              <p className="text-4xl font-bold text-green-400">{slide.retorno}</p>
            </div>
          </motion.div>

          {/* Patrocínios e Papel dos Sócios */}
          <div className="grid grid-cols-2 gap-8">
            <motion.div
              initial={{ opacity: 0, y: 20 }}
              animate={{ opacity: 1, y: 0 }}
              transition={{ delay: 0.7 }}
              className="bg-purple-900/20 border border-purple-700 rounded-xl p-6"
            >
              <h4 className="text-2xl font-bold text-purple-400 mb-4">{slide.patrocinios.title}</h4>
              <div className={`${getTextSize('base')} text-gray-300 space-y-2`}>
                {slide.patrocinios.items.map((item, i) => (
                  <p key={i}>• {item}</p>
                ))}
              </div>
            </motion.div>

            <motion.div
              initial={{ opacity: 0, y: 20 }}
              animate={{ opacity: 1, y: 0 }}
              transition={{ delay: 0.8 }}
              className="bg-green-900/20 border border-green-700 rounded-xl p-6"
            >
              <h4 className="text-2xl font-bold text-green-400 mb-4">{slide.papelSocios.title}</h4>
              <div className={`${getTextSize('base')} text-gray-300 space-y-2`}>
                {slide.papelSocios.items.map((item, i) => (
                  <p key={i}>• {item}</p>
                ))}
              </div>
            </motion.div>
          </div>
        </div>
      </div>
    );
  }

  return null;
};
